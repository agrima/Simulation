Do "make" and an executable "sim" will be generated.

Since it require root privilege to change the thread priority, so this program can only run correctly on machine will you can do a "sudo ./sim".