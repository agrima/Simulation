#define SELF -1

class asim {
	public:
		asim();
		void create(void *funcname(void *v));
		void yield(int thread);
		void setpri(int thread, int pri);
		void hold(float time);
		int sched_tid;
};